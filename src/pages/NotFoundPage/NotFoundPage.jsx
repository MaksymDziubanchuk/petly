import scss from "./not-found-page.module.scss";

const NotFoundPage = () => {

    return (
        <section className={`${scss.notfound__section} ${scss.container}`}>
        </section>
    )
}

export default NotFoundPage;